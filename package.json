{
    "name": "node-p3",
    "version": "1.0.0",
    "scripts": {
        "test": "mocha test/*.test.js",
        "build": "echo Building..."
    },
    "devDependencies": {
        "mocha": "^10.0.0"
    }
}
